logging:
  file:
    path: /app/logs

server:
  port: 6324
  servlet:
    context-path: /lfs-code-assistant

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://8.148.146.195:3336/lfs?useUnicode=true&characterEncoding=UTF-8&serverTimezone=Asia/Shanghai
    username: root
    password: ${MYSQL_PASSWORD}
    hikari:
      minimum-idle: 10
      maximum-pool-size: 10
      connection-timeout: 30000
      max-lifetime: 1800000
      idle-timeout: 600000
      keepalive-time: 300000
  main:
    allow-circular-references: true

mybatis-plus:
  configuration:
    default-enum-type-handler: org.apache.ibatis.type.EnumOrdinalTypeHandler
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
    cache-enabled: true
  global-config:
    db-config:
      id-type: auto
      logic-delete-field: deleted
      logic-not-delete-value: 0
      logic-delete-value: 1
  mapper-locations: classpath:mapper/*.xml

jwt:
  secret: a7d9f8b3c2e1a6f0d9c8b7a6d5e4f3a2b1c0d9e8f7a6b5c4d3e2f1a0b9c8d7e61145141919810

app:
  file:
    storage-path: /app/data/files  # 容器内部的路径
    aes-key: LfsCodeAssistKey1145141919810!@# # 必须16或32位